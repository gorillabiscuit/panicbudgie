# build / cache
node_modules
dist
.vite
.DS_Store
*.log

# Vercel project link is local-only
.vercel

# Personal tooling state
.agents
.claude
skills-lock.json

# Heavy source assets (PSDs and source-of-truth folder are kept local).
# `/character` is root-anchored so the curated `style-guide/character/`
# subset stays tracked.
/character
*.psd
*.3dm

# Client docs (kept local)
*.docx
~$*.docx

# Local screenshots / scratch at repo root
/panicbudgie-*.png
